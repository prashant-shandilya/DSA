#include<iostream>
#include<vector>
using namespace std;
//To check if there is a loop in a directed graph.

bool checkCycle(vector<int>graph[],vector<bool> &visited,vector<bool>trail,int indx){
  visited[indx] = true;
  trail[indx] = true;
// cout<<indx<<endl;
  for(auto key:graph[indx]){
    if(!visited[key]){
      if(checkCycle(graph,visited,trail,key))
        return true;
    }
    else if (trail[key]){
      cout<<indx;
      cout<<key;
    return true;      
    }
  }
    trail[indx] = false;
    
return false;
}

int main(){
vector<int> graph[7];

  graph[0].push_back(1);
  graph[1].push_back(0);
  // graph[1].push_back(4);
  // graph[1].push_back(2);
  // // graph.push_back({{3,4}});
  // graph[2].push_back(3);
  // graph[2].push_back(4);
  
  // // graph.push_back({1});
  // graph[3].push_back(1);
  // // graph.push_back({5,6});
  // graph[4].push_back(5);
  // graph[4].push_back(6);
  // // graph.push_back({6});
  // graph[5].push_back(6);

  
  vector<bool> visited;
  vector<bool> trail;
  for(int i=0;i<6;i++){
    visited.push_back(false);
    trail.push_back(false);
  }
  
  for(int i=0;i<6;i++){
    if(!visited[i]){
     if(checkCycle(graph,visited,trail,i)){cout<<"true";break;}
    }
  }
  // cout<<"False";

}
**********************************************************************************************************************************************************************
there is a visited and a trail bool array.
if we visit a node then put it in the visited array , and in the trail array this means that we have visted and is in the current trail. if for a node
we have a node which is going to it and is both visited and trailed then we find our cycle.
