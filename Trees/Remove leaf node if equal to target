// Remove the leaf node if it is equal to the target.

// Using Post  Order traversal we can achieve this in O(n) time complexity.

class Solution {
public:
    TreeNode* removeLeafNodes(TreeNode* root, int target) {
        
        if(root->left != nullptr){
            root->left = removeLeafNodes(root->left,target);
        }
        
        if(root->right != nullptr){
            root->right = removeLeafNodes(root->right,target);
        }

        // if 3,3,3 and tar=3 then when operating on the root it needs to be deleted.
        if(root->left == nullptr && root->right == nullptr && root->val == target){
            return nullptr;
        }
        else {
        return root;
        }
        
    }
};
