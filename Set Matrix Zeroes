var setZeroes = function(matrix) {
    
    let col0 = false;
    
    for(let i=0;i<matrix.length;i++){
        if(matrix[i][0] === 0) col0 = true;
        
        for(let j=1;j<matrix[0].length;j++){
            if(matrix[i][j] == 0){
                matrix[i][0] = 0;
                matrix[0][j] = 0;
            }
        }
    }
    
    for(let i=matrix.length-1;i>=0;i--){
        for(let j=matrix[0].length-1;j>=1;j--){
            if(matrix[i][0]==0||matrix[0][j]==0)matrix[i][j]=0;
        }
        if(col0){
            matrix[i][0]=0;
        }
    }
    
    *****************************************************************************************************************************************************
    in this question if we encounter a 0 in a cell then put all the row's element to 0 and the corresponding column element to 0 also.
    
    so for the first row the 0,0 indicates the position of the flag if it is 0 then the entire row should be zero.
    
    and similarly if the cell is zero then put the top element to 0 which represents this column is going to be all zero
    
    for the first col we have other var clled col0 which if any element in the first col is 0 then only is activated.
    
    the second run we go from bottom to top. and avoid first column 
