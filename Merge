Given an array of intervals where intervals[i] = [starti, endi],
merge all overlapping intervals, and return an array of the non-overlapping intervals that cover all the intervals in the input.



 vector<vector<int>> merge(vector<vector<int>>& intervals) {
        
        if(intervals.size()<=1)return intervals;
        sort(intervals.begin(),intervals.end(),go);
        int size = intervals.size()-1;
        vector<vector<int>> ans;
        
        ans.push_back(intervals[0]);
        
        for(int i=1;i<=size;i++){
            
            if(ans.back()[1] >= intervals[i][0]){
                ans.back()[1] = max(ans.back()[1],intervals[i][1]);
            }
            else{
                ans.push_back(intervals[i]);
            }
        }
        return ans;
        
    }
    
    ********************************************************************************************************************************************************************
    put the first element of intervals after sorting. Compare the second element of the last element of the ans with the first element of the current element of the 
    intervals.
    if it is bigger or equal to then put the bigger of the last element of the last element of the ans and the last ele of the current intervals element in the last
    ele of the last ele of the ans;
    
    else we push back the current element of the interval.
