Given n pairs of parentheses, write a function to generate all combinations of well-formed parentheses.

 vector<string> ans;
    vector<string> generateParenthesis(int n) {
        
     helper(n,0,"");
        return ans;
    }
    void helper(int n,int m,string s){
        if(m>0){helper(n,m-1,s+")");}
        if(n>0){helper(n-1,m+1,s+"(");}
        
        if(n==0&&m==0){
            ans.push_back(s);
        }
        
// maintain two numbers n (for number of " ( " left) and m (for number of " ) " left) n starts from n and m starts from 0. if n==0 and m==0 then push_back str;
